{
  "name": "bradify",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n FRONTEND,DESKTOP,BACKEND -c green,blue,magenta \"pnpm --filter frontend dev\" \"pnpm --filter desktop dev\" \"pnpm --filter backend dev\"",
    "build": "pnpm --filter frontend build && pnpm --filter backend build && pnpm --filter desktop build",
    "start": "pnpm --filter desktop start"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "pnpm": "^8.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.1"
  }
}